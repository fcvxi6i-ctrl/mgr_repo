

models:
  - name: "ff2.sql"
    config:
      materialized: 'table'


sources:
  - name: "mgr"
    database: DB_ECOMM
    schema: SC_ECOMM
    tables:
      - name: users
      - name: sales